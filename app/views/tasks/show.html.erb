<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= link_to "show all tasks", tasks_path %>
<%= link_to "edit task", edit_task_path(@task.id) %>

<%= link_to "Delete this task", task_path(@task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
